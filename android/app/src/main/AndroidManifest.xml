<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ============================================================ -->
    <!-- 1. PERMISOS DEL SISTEMA I HARDWARE                           -->
    <!-- ============================================================ -->

    <!-- API CONNECTIVITY: Necessari per connectar a l'API d'Open-Meteo -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- GEOLOCALITZACIÓ: Permet obtenir Lat/Lon per contextualitzar la dada climàtica -->
    <!-- FINE: Precisió GPS alta. COARSE: Precisió per antenes/Wifi. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <!-- LLANTERNA: Feedback visual per a entorns sorollosos -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- VIBRACIÓ: Feedback hàptic (Haptics Plugin) -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- NOTIFICACIONS: Obligatori a partir d'Android 13 (API 33) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- SCHEDULE_EXACT_ALARM: Per garantir precisió mèdica en el recordatori -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- ============================================================ -->
    <!-- 2. CONFIGURACIÓ DE L'APLICACIÓ                               -->
    <!-- ============================================================ -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- FILE PROVIDER: Necessari per a la gestió interna de fitxers de Capacitor -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>